# Copy to .env.local for local dev (not needed — vite.config.ts proxy handles it)
# Set this in Vercel dashboard under Project → Settings → Environment Variables
VITE_API_URL=https://your-backend.onrender.com
